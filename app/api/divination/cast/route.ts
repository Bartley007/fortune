import type {CastingMethod,DivinationCastRequest} from "@/lib/contracts/divination";import {failure,record,success,text} from "@/lib/contracts/api";import {castDivination} from "@/lib/divination/service";
export async function POST(request:Request){let body:Record<string,unknown>|null=null;try{body=record(await request.json())}catch{}if(!body)return failure("divination-cast-v1","INVALID_JSON","请求体必须是 JSON 对象。");const question=text(body.question),method=String(body.method??"");if(!question||!["numbers","random","coins"].includes(method))return failure("divination-cast-v1","VALIDATION_ERROR","请提供 question 和有效的 method。");const numbers=Array.isArray(body.numbers)?body.numbers.filter((x):x is number=>typeof x==="number"&&Number.isFinite(x)).slice(0,3):undefined;if(method==="numbers"&&(!numbers||numbers.length<2))return failure("divination-cast-v1","VALIDATION_ERROR","数字起卦至少需要两个数字。");const input:DivinationCastRequest={question,method:method as CastingMethod,numbers,time_range:text(body.time_range,80)||undefined};try{const output=await castDivination(input);return Response.json(success(output.data,{system:"divination-cast-v1",sessionId:crypto.randomUUID(),warnings:output.warnings,mock:output.mock}))}catch(error){return failure("divination-cast-v1","ALGORITHM_SERVICE_ERROR","Python 起卦服务调用失败。",{cause:error instanceof Error?error.message:"unknown"},502)}}
