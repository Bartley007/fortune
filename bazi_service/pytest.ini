[pytest]
# Put the project root on sys.path so `from app.main import app` resolves when
# tests are run as plain `pytest` (which, unlike `python -m pytest`, does not
# add the working directory itself).
pythonpath = .

testpaths = tests